//header file in the C standard library that provides functions for input and output operations.
#include<stdio.h>

//create a node
struct Sllnode
{
    //to store Data
    int data;
    //to store Next Node Address
    struct Sllnode* next;
};

//Starting with the head node
int create_sll_head()
{
    head = 
}
//The main function is an important part of a C program. It is the starting point of a program's execution and is where the program's instructions are executed.
int  main()
{
    //Call functions and pass values to the sll function
}